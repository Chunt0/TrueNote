/** App display name (browser title + sidebar/topbar). Set via VITE_APP_NAME. */
export const APP_NAME = import.meta.env.VITE_APP_NAME?.trim() || 'putty-ai'

/** Brand slogan — the mono whisper under the wordmark. */
export const APP_SLOGAN = 'Soft. Local. Yours.'
